export interface MyFormData {
    formData: string;
    name: string;
    email: string;
    phone: string;
    message: string;
    blockList: string | null;
    leafList: string | null;
    tubesList: string | null;
}